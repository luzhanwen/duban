# 读伴 QA 矩阵与回归样本

> 最后更新：2026-07-23

本文档承接 P6.10「QA 矩阵与回归样本」。目标是让每次发布前的人工验证不再依赖记忆，而是按固定场景、固定预期和固定证据记录执行。

## 使用规则

- 发布前至少执行「P0 Smoke Test」。
- 改动触及某个模块时，额外执行该模块对应的 P1 回归场景。
- 涉及 SQLite schema、备份、导入恢复或 App 数据目录时，必须执行「升级与数据恢复」场景。
- 涉及 AI transport、Keychain、预算、模型 profile 或诊断日志时，必须执行「AI 与设置」场景。
- 测试证据可以是简短文字、脱敏截图或诊断摘要；不得包含 API Key、书籍正文、私密笔记、聊天全文、绝对本地路径或未授权版权内容。
- 结果标记统一使用：`Pass`、`Fail`、`Blocked`、`Skipped`。

## 测试环境维度

| 维度 | 必测值 | 发布前说明 |
| --- | --- | --- |
| App 形态 | 浏览器版、Tauri 桌面版 | P6 发布重点是桌面版；浏览器版用于确认未被破坏 |
| 包类型 | dev、local DMG、signed DMG | Developer ID 链路已打通；正式候选必须执行 signed DMG 验收 |
| 数据通道 | test、formal | SQLite/files/backups/diagnostics/Keychain 必须隔离；测试数据不得出现在 formal |
| 用户状态 | 新用户、已有用户 | 新用户验证首次启动；已有用户验证数据恢复 |
| 网络状态 | 有网络、无网络 | AI 请求仅有网络时验证；无网络验证错误提示 |
| API Key | 未配置、正确 Key、错误 Key | 正确 Key 用人工私密环境验证 |
| 文件类型 | PDF、MOBI | 扫描版 PDF 当前记录为已知限制 |
| 数据规模 | 小书、中等书、大书 | 大书样本不提交版权文件，只记录本地样本名与特征 |
| 备份来源 | 新备份、旧备份、损坏备份 | P6.10.2 已提供空备份和篡改备份 manifest；含书升级样本进入 P6.10.3 |

## P0 Smoke Test

发布候选包必须先通过这些场景，才继续更细的回归测试。

| ID | 场景 | 步骤 | 预期结果 | 证据 |
| --- | --- | --- | --- | --- |
| SMK-001 | 首次启动 / AI 未配置 | 打开 App | 开屏 Logo 缩小并落到欢迎页 Logo 的同一位置，过程中只有一个 Logo；欢迎页无右上角关闭按钮、无空白帧或二次闪入；开始设置后默认推荐 DeepSeek | 截图或文字 |
| SMK-001A | 首次启动 / AI 已配置 | 已保存任一供应商 Key 后重启 | 不显示首次 AI 引导，不主动读取 Keychain 明文，不触发系统密码框 | 截图或文字 |
| SMK-002 | 书架显示 | 查看书架和顶部入口 | 书架正常渲染，设置、下载桌面版入口不遮挡主流程 | 截图 |
| SMK-002A | 大书库响应式网格 | test channel 使用 `?shelf-grid-fixture=20`，并在宽、中、960px 桌面最小窗口检查书架 | 20 本书按可用宽度自动增减列数；所有行书卡等高，短/长标题、作者、状态和进度不引起错位；无横向溢出，封面比例不变 | 各宽度列数与卡片高度摘要 |
| SMK-002B | 书卡阅读状态 | 分别检查未开始、今天开始但未完成、今天完成阅读项、整本完成的书 | 依次显示「未启卷 / 今日未读」「阅读中 / 今日在读」「今日完成」「全书读完」；发生阅读活动不得被误写为完成 | 状态文案或截图 |
| SMK-002C | 网页超宽书架 | 在 1280px、2K 或更宽浏览器查看导航与 20 本书夹具 | 导航和书架框架使用完整可用视口并保留安全边距；卡片不被拉宽、列数随空间增加，无横向溢出；桌面 App 布局不变 | 框架宽度、列数与截图 |
| SMK-003 | 导入 PDF | 导入一本可公开或本地授权 PDF | 书籍进入书架，能打开阅读器 | 书名/页数摘要 |
| SMK-003A | 已保存 PDF 兼容读取 | 使用升级前已经进入 App 数据目录的 PDF，完全退出后重新打开并继续阅读 | 不重新导入即可读取原页，恢复历史页码，PDF 原页与文本层正常；不得出现 `asset://` 状态 0 | 脱敏截图或页码摘要 |
| SMK-004 | 导入 MOBI | 导入一本可公开或本地授权 MOBI | 书籍进入书架，章节可打开 | 章节数摘要 |
| SMK-005 | 阅读器打开 | 打开 PDF 和 MOBI 阅读器 | 正文、页码或章节、右侧读伴区域正常 | 截图 |
| SMK-006 | 阅读进度 | 翻页或切换章节后退出再进入 | 进度恢复到最近位置 | 文字 |
| SMK-007 | API Key 状态 | 打开设置页 | 已保存 Key 有明确状态提示；不会自动弹连续系统密码框 | 截图/文字 |
| SMK-008 | AI 请求 | 使用正确 Key 发起一次简短问答 | 返回内容，失败时错误可读且不泄露 Key | 脱敏摘要 |
| SMK-009 | 备份导出 | 导出一次备份 | 生成目录式备份，manifest 与 files 存在 | 文件摘要 |
| SMK-010 | 重启恢复 | 完全退出 App 后重新打开 | 书库、进度、笔记/聊天摘要和设置状态恢复 | 文字 |

## P1 核心回归矩阵

### 书库与导入

| ID | 场景 | 触发条件 | 预期结果 |
| --- | --- | --- | --- |
| LIB-001 | 重复导入同一本书 | 导入同一 PDF/MOBI 两次 | 不破坏已有数据；重复策略符合当前产品行为 |
| LIB-002 | 取消导入 | 大文件导入中取消 | 取消后无半成品书籍或孤儿文件异常 |
| LIB-003 | 导入失败 | 导入坏文件或不支持格式 | 失败提示友好，不写入损坏数据 |
| LIB-004 | 大书导入 | 导入接近当前限制的大 PDF/MOBI | 显示进度、可取消、错误提示可理解 |
| LIB-005 | 章节识别 | 打开带目录和不带目录的 PDF/MOBI | 章节列表可用；低质量识别可人工调整 |
| LIB-006 | 混合 MOBI/KF8 | 导入扩展名为 `.mobi`、内部同时含旧 MOBI 与 KF8/AZW3 内容的本地授权样本 | 选择结构更完整的解析结果，不得只保存旧 MOBI 残缺壳；版权/目录和含子章节的卷标题为忽略，无标题正文并回上一真实章节；页数、章节和正文量符合文件内容 |

### 阅读器与笔记

| ID | 场景 | 触发条件 | 预期结果 |
| --- | --- | --- | --- |
| RD-001 | PDF 翻页 | 连续翻页、跳页 | 页面稳定渲染，无明显重排或空白 |
| RD-001A | PDF 自适应单页 | 在不同窗口尺寸切到翻页模式，并收起/恢复读伴侧栏 | 单页完整居中，无纵向滚动；比例、文本层和高亮对齐，页码不变 |
| RD-001B | PDF 多入口翻页 | 分别使用边缘点击、方向键、触摸滑动和触控板横滑 | 每次只翻一页，可用方向正确，页码与继续阅读进度同步 |
| RD-001C | 翻页动画开关 | 开启动画翻页，再关闭并重开书籍 | 开启时方向正确且轻量；关闭后即时切页，偏好保留；reduced motion 不播放 |
| RD-001D | 窄窗口阅读 | 在 960px 左右窗口分别使用滚动/翻页，收起并重新打开读伴 | 标题不竖排；书页不被侧栏压缩；滚动正文保持可读宽度；读伴以覆盖层打开 |
| RD-001E | 阅读项相对页码 | 打开 startPage 大于 1 的阅读项并连续翻页 | 起始页显示本节第1页并保留原书页码；后续相对递增；存储与笔记仍使用原书页码 |
| RD-002 | MOBI 章节阅读 | 切换章节、滚动正文 | 正文不丢段，章节标题稳定 |
| RD-002A | MOBI 自适应翻页 | 打开含长文本页的 MOBI，分别在带读伴、专注阅读和约 960px 窄窗口连续正向/反向翻页 | 逻辑文本页按可用宽高拆成多屏，字号不被压小且无底部截断/右侧串栏；所有翻页入口先移动屏幕页，跨逻辑页后反向可回到上一页最后一屏；进度仍记录逻辑文本页 |
| RD-002B | MOBI 页边精确划词 | 在翻页模式拖选靠近书页左右边缘的 1–4 个汉字，再清除选择并分别轻点左右边缘 | 拖选只包含目标文字，不被扩展到后续段落且不会误翻页；无文字选择的页边单击仍只翻一屏，方向和计数正确 |
| RD-003 | 高亮与笔记 | 分别在 PDF、MOBI 的滚动/翻页模式选中文字并添加笔记，再保存、切换屏幕页、重开书籍、取消高亮和重新划原文 | 待保存草稿即时高亮；保存并重开后仍定位原文；MOBI 自适应分屏或窗口变化不漂移；取消高亮后正文标记消失，重新划原文后移动到新位置 |
| RD-004 | 章节编辑 | 修改章节标题或范围 | 修改后阅读入口和导读上下文使用新信息 |
| RD-005 | 读后交流 | 完成一次读后交流 | 对话保存到本书上下文 |
| RD-006 | 读伴收起与唤醒 | 在滚动/翻页模式输入未发送草稿，收起读伴，再点击页边猫耳书页印记 | 工具栏不出现第二个文字唤醒入口；侧栏原位恢复，草稿、页码、模式和时间线状态不丢失 |
| RD-006A | 读伴响应式开合 | 在 1280px、960px、760px 分别收起和唤醒读伴 | 无横向溢出；窄窗侧栏覆盖展开而不压缩正文；印记点击区域至少 44px |
| RD-006B | 划词自动唤醒 | 读伴已收起时选中文字并选择「问读伴」 | 侧栏自动展开并进入问答，引用内容保留，不触发主动模型调用 |
| RD-006C | 减少动态 | 系统开启减少动态后收起、唤醒读伴 | 状态可正常切换，但不播放印记和侧栏入场动画 |
| RD-006 | 阅读工具栏图标 | 检查阅读中和完成后状态 | 中途离开为茶杯，回到书架为书本，我读完了为圆形勾选；阅读页不存在主动提问入口或无关印章复用，图标与文字对齐 |

### AI 与设置

| ID | 场景 | 触发条件 | 预期结果 |
| --- | --- | --- | --- |
| AI-001 | 未配置 Key | 发起 AI 请求 | 提示去设置，不崩溃 |
| AI-002 | 错误 Key | 使用错误 Key 请求 | 结构化错误可读，不泄露 Key |
| AI-003 | 正确 Key | 生成导读/问答 | 请求成功，预算与 token 提示正常 |
| AI-004 | 请求取消 | 请求中取消 | UI 停止等待，后端记录取消摘要 |
| AI-005 | 自定义 Base URL | 使用 OpenAI-compatible 自定义地址 | URL 校验和错误提示符合预期 |
| AI-006 | 模型 profile | 切换 profile 后请求 | 使用当前 profile 的模型、价格和预算设置 |
| AI-007 | Keychain 状态 | 设置页保存、留空保存、更新 Key | 留空不覆盖已有 Key，新 Key 才更新 |
| AI-008 | Keychain 通道隔离 | 分别在 test/formal 保存或读取 Key | 两个 identifier 使用独立 service，互不显示或覆盖 |
| AI-009 | 导读截断自动恢复 | 首次导读返回 `finish_reason=length` 或不可用结构，第二次返回完整 JSON | 自动要求压缩成品并以更高但受控的上限重试一次；最终导读可用，两次 token/费用合并记录，不保存半截内容 |
| AI-010 | 导读连续失败 | 两次导读都被截断或无法形成有效结构 | 停止重试并显示可操作错误；不写入缓存，允许用户重新生成或调整导读输出上限 |
| AI-011 | 导读输入超限恢复 | 首次请求明确返回 `AI_CONTENT_TOO_LONG`，压缩后请求成功 | 只重组一次约 55% 预算的上下文；长正文保留首、中、尾，诊断标记压缩信息，网络/鉴权错误不触发该流程 |
| AI-012 | 导读表达可读性 | 为历史、理论或制度类章节生成首项和后续导读 | 能直接说清上一项、当前项和阅读重点；每段有具体对象，术语有白话解释；按完整句子判断，不使用词语黑名单 |
| AI-013 | 导读阅读动机 | 为连续阅读中的后一项生成导读 | 上一项只承接 1 句话；当前项用一个具体冲突、反常或后果建立阅读动机；文风不触发自动重写或额外费用 |
| AI-014 | 用词替代偏好 | 在名单加入词条后分别构造导读、问答、读后、总结、整本书导读和正文排版 prompt | 生成类 prompt 统一带入名单；正文排版不带入；名单只提供语义偏好，不机械替换用户输入、书籍原文、引文或专有名词 |

### 备份、导入与诊断

| ID | 场景 | 触发条件 | 预期结果 |
| --- | --- | --- | --- |
| BK-001 | 备份导出 | 有书籍、笔记、聊天后导出 | manifest、files、sha256 完整；不包含 API Key |
| BK-002 | 导入前预览 | 选择备份目录 | 显示书籍数量、文件状态、风险摘要 |
| BK-003 | 校验报告 | 导入前运行校验 | 缺失、篡改或版本问题有明确报告 |
| BK-004 | 合并导入 | 导入到已有书库 | 不覆盖未在备份中的书；冲突策略符合预期 |
| BK-005 | 覆盖恢复 | 使用备份覆盖当前数据 | 失败时可回滚；成功后数据一致 |
| BK-006 | 诊断包 | 设置页导出诊断包 | 诊断包不含正文、Key、prompt、笔记全文或绝对路径 |
| BK-007 | 陪读事件合并 | 本地事件较新时导入含同 id 旧事件的备份，再重复导入 | 不重复事件，不覆盖较新本地状态；同时间 tombstone 优先 |
| BK-008 | 陪读事件隐私校验 | 导入事件缺 id/状态、bookId 不匹配或 `sourceAnchor.text` 含原文的备份 | 预览报告明确失败，当前书库不变 |

### 发布包与桌面行为

| ID | 场景 | 触发条件 | 预期结果 |
| --- | --- | --- | --- |
| REL-001 | local DMG | `npm run package:mac-local` | 生成命名规范的 local DMG |
| REL-002 | formal channel | `npm run release:preflight` | formal dist 不含测试书或测试入口 |
| REL-003 | 窗口关闭 | 点击窗口叉号 | 主窗口隐藏到后台，不直接退出 |
| REL-004 | Dock 唤回 | 点击 Dock 图标 | 主窗口重新显示并聚焦 |
| REL-005 | 系统退出 | `Cmd+Q` 或菜单退出 | App 真正退出 |
| REL-006 | signed DMG | Apple 证书可用后执行 | signed/notarized/stapled/Gatekeeper 均通过 |
| REL-007 | test/formal 数据隔离 | test 中保留书库后启动 formal 新用户包 | formal 书架为空且不读取 test SQLite/files/backups；test 数据仍完整 |

## 升级与数据恢复

| ID | 场景 | 数据来源 | 预期结果 |
| --- | --- | --- | --- |
| UPG-001 | 新 schema 空库启动 | 删除或隔离 App 数据目录后启动 | 自动初始化到当前 schema |
| UPG-002 | 旧 schema 数据库升级 | P6.10.3 准备的旧库样本 | 迁移成功，版本更新，核心数据保留 |
| UPG-003 | 旧备份导入 | P6.10.3 准备的旧备份样本 | 预览、校验、导入路径可用 |
| UPG-004 | 新备份 roundtrip | 当前版本导出后导入 | 数据一致，文件 sha256 一致 |
| UPG-005 | 损坏备份 | 篡改 manifest 或文件 hash | 校验失败且不破坏当前书库 |
| UPG-006 | schema 9 -> 10 陪读事件迁移 | 含导读、问答、笔记和读后记录的旧库 | 升级成功；原记录仍可见，惰性事件 id 稳定且不复制正文 |
| UPG-007 | schema 10 事务回滚 | 写入重复事件 id 或错误 bookId 的事件数组 | 整组写入失败，原事件集保持不变 |
| UPG-008 | P7.6 旧阅读进度兼容 | `reading_progress.raw_json` 不含 `readStateByItemKey` | 正常打开并按空已读范围处理，不要求迁移或重新导入 |
| UPG-009 | P7.6 旧来源定位兼容 | 仅含页码/字符范围的 v1 来源定位 | 继续按 v1 读取；新定位字段为空，不丢失原页码 |

## P7.6 已读范围与来源定位

| ID | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| RDG-001 | 正常连续阅读 | 打开本节第一页并停留超过 2 秒，再进入下一页 | 第一页记录为确认已读，下一页先只记录为到达 |
| RDG-002 | 跳页 | 从第一页直接跳到第三页 | 第二页不会被补记为已读 |
| RDG-003 | 严格防剧透 | 设置“不透露未读内容”，在第二页提问 | 请求只包含当前页和此前确认已读的合格内容，不含第三页或整节正文 |
| RDG-004 | 低质量文本 | 打开空白、乱码、目录或数字密集页 | 阅读不受阻；该页不进入历史正文放行或按需上下文缓存 |
| RDG-005 | 来源回找 | 为选区保存笔记后重新解析相同或轻微变化文本 | 优先按正文块精确定位；轻微变化可按指纹/选文回找；失败明确标为失效 |
| RDG-006 | 完成本节 | 点击“我读完了” | 当前阅读项全部合格正文块记为已读，并保留完成时间 |

## P7.7 按需上下文与缓存

| ID | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| CTX-001 | 严格防剧透问答 | 当前页、已读页和未读页放入固定样本后提问 | 请求只含选区、当前页和确认已读块；未读正文、导读和旧模型回答不出现 |
| CTX-002 | 只给方向 | 将剧透规则改为“只给方向”后提问 | 仍不带入未读正文或具体后文结果，只允许抽象提示 |
| CTX-003 | 允许讨论后文 | 单次选择允许后文后提问 | 当前阅读项全文、导读和允许的历史对话可进入请求，来源仍有指纹 |
| CTX-004 | 来源追踪 | 检查生成结果的 `contextTrace` | 含用途、页码、正文块 id、内容指纹、质量和排除原因，不含正文全文 |
| CTX-005 | 三档预算 | 分别选择简短、适中、深入 | 上下文字符预算、结构指令和输出 token 上限稳定递增；profile 不能抬高硬上限 |
| CTX-006 | 缓存命中 | 用相同正文、策略、模型和 prompt 重复组装 | 第二次命中；cache key 相同 |
| CTX-007 | 缓存失效 | 分别改变正文、策略、模型、prompt 版本或导读连续性 | 每种变化都会产生新 cache key；旧制品不作为本次命中 |
| CTX-008 | 用户明确重生成 | 已有匹配导读后点击“重新生成导读” | 显式绕过完整制品缓存并发起新请求 |
| CTX-009 | 读后边界 | 阅读项未完成时直接构造读后请求 | 不带入整项未读正文；确认完成后才可带入本项全文 |
| CTX-010 | 禁止自动调用 | 翻页、停留、高亮、切换窗口 | 只更新阅读状态或来源，不调用导读、问答或读后模型 |

### 本书记忆长期回归

| ID | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| MEM-001 | 跨章节查看 | 在「整理这本书」打开“记忆” | 按阅读顺序显示来源类型、来源阅读项和记忆内容 |
| MEM-002 | 修改章节记忆 | 修改一条来自本节记录的记忆并保存 | 本书记忆与 `memoryLink.text` 同步，来源关联不丢失 |
| MEM-003 | 撤销章节记忆 | 二次确认撤销 | 记忆移除、`memoryLink` 清空；本节记录及问答/笔记/读后正文仍存在 |
| MEM-004 | 旧记录补全 | 打开含旧 `memoryLink` 但缺少来源字段的书 | 只补齐可唯一确认的 `sourceItemKey/sourceEventId`；`legacy` 内容不升级 |
| MEM-005 | 备份合并 | 重复合并 `backup-companion-events-v3` | 两条跨阅读项记忆保持来源；事件去重；删除 tombstone 不复活 |

## P7.10 读伴状态与动效

| ID | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| VIS-001 | 安静陪读 | 进入正文且不发起 AI 请求 | shell 与三种形象规格均为 `quiet`；不闪烁、不呼吸、无红点或循环动画 |
| VIS-002 | 导读生成 | 点击生成导读，再停止或等待完成 | 运行期间为 `preparing`；取消/完成后立即回到等待态，不残留进度反馈 |
| VIS-003 | 读中/读后回答 | 主动发送问题或整理总结 | 请求期间为 `answering`；流式结束或取消后退出任务态 |
| VIS-004 | 保存阅读记录 | 保存选区笔记、回答笔记或更新笔记 | 成功后进入一次性 `recording` 反馈；失败不伪装成功 |
| VIS-005 | 完成阅读 | 完成本节并进入完成页 | 状态为 `complete`，只播放一次短过渡 |
| VIS-006 | 错误 | 导读、读中或读后请求返回错误 | 当前场景为 `error`，保留原错误文案和重试/取消能力，无循环警示动画 |
| VIS-007 | 离线 | 断开网络后在导读、正文和读后切换 | 离线优先于其他视觉状态；明确说明 AI 暂不可用，阅读、翻页、笔记和已有内容不受阻 |
| VIS-008 | 减少动态效果 | 系统开启“减少动态效果”后重复以上操作 | 状态文案和功能完整，形象、状态点、进度线与共享过渡均不播放动画 |
| VIS-009 | 窄窗口 | 在 390×844 打开导读、正文和离线提示 | 无横向溢出；提示不遮挡主要操作；收起印记继承当前真实状态 |

## P7.11 诊断与候选验收

| ID | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| DIA-001 | 上下文解释 | 在设置页展开最近一次导读、问答或读后调用 | 能看到材料类型、页码、字符数、排除原因、规则、预算和缓存状态，不显示正文 |
| DIA-002 | 制品缓存 | 连续进入相同导读 | 第二次记录为“缓存复用/直接使用已生成导读”，不产生模型 token 或费用 |
| DIA-003 | 摘要隐私 | 用秘密标记构造来源正文、笔记、prompt、消息和排除说明 | 存储、复制摘要和导出投影均不包含秘密标记或原始 id |
| DIA-004 | 诊断包隐私 | 导出后搜索当前书籍、笔记、问题、回答与 API Key 原句 | 均不命中；URL 只保留 origin，敏感 key 二次替换为 `[redacted]` |
| DIA-005 | 正式包边界 | 执行 formal build、P7 preflight 与 release preflight | 正式 dist 不含测试入口、固定样本、测试秘密、证书或私钥材料 |
| DIA-006 | 不主动调用 | 翻页、停留、高亮、窗口切换后刷新诊断 | 没有新增 AI 调用，界面没有主动提问入口 |
| DIA-007 | 固定案例 | 执行 `qa:fixtures:verify` 与 `test:p7-qa-cases` | 七类合成案例齐全、顺序稳定且无私人/版权内容 |

## 回归样本策略

- 不提交版权受限 PDF/MOBI 原文。
- 可提交的样本必须来自公版、开源授权、用户自写或极小合成文件。
- 大书、坏书、扫描版 PDF 等样本可以先记录在本地清单，不进入仓库。
- 每个样本记录这些元信息：文件类型、页数/章节数、大小、是否扫描版、用途、预期结果。
- 固定样本维护在 `qa-fixtures/`，用 `npm run qa:fixtures` 生成，用 `npm run qa:fixtures:verify` 验证。

## P6.10.2 固定 Fixtures

| ID | 路径 | 用途 | 预期 |
| --- | --- | --- | --- |
| `pdf-valid-two-page` | `qa-fixtures/books/duban-qa-two-page.pdf` | PDF 导入、阅读器打开、翻页 smoke test | 可被 PDF.js 读取为 2 页 |
| `pdf-corrupt-negative` | `qa-fixtures/books/duban-qa-corrupt.pdf` | 导入失败负向测试 | PDF 解析失败，App 应给友好错误 |
| `mobi-source-html` | `qa-fixtures/books/duban-qa-mini-book.html` | 后续生成合法 MOBI 的源文本 | 不是可导入 MOBI |
| `backup-empty-v3` | `qa-fixtures/backups/duban-backup-empty-v3/manifest.json` | 备份预览和校验 smoke test | manifest hash 正确；merge import 是 no-op |
| `backup-tampered-v3` | `qa-fixtures/backups/duban-backup-tampered-v3/manifest.json` | 备份校验负向测试 | manifest hash 故意错误 |
| `backup-companion-events-v3` | `qa-fixtures/backups/duban-backup-companion-events-v3/manifest.json` | P7.5/P7.9.3 事件、跨章节记忆、删除状态、去重合并和 schema 迁移 | 含两条合成记忆、两个有效本节记录和一个 tombstone；不含真实正文；事件 id 和 hash 稳定 |
| `p7-context-cases` | `qa-fixtures/p7/companion-context-cases.json` | P7.11 普通 PDF、无目录、扫描页、长章节、旧书、MOBI、窄窗口契约 | 七类案例完整；只含合成元数据和预期，不含真实正文或私人数据 |

命令：

```bash
npm run qa:fixtures
npm run qa:fixtures:verify
```

说明：

- `qa-fixtures/fixtures.json` 记录每个 fixture 的路径、大小、sha256、用途和预期。
- 当前不提交二进制 MOBI fixture。MOBI 用本地授权样本人工验证，并在 QA Run 里记录文件名、大小、章节数和来源授权摘要。
- `backup-empty-v3` 是空备份。人工测试 replace import 会清空当前数据，通常只用于预览或 merge import。
- 含真实书籍数据的备份样本和旧 schema 升级样本放到 P6.10.3。

## 2026-07-23 合成 PDF 桌面 QA Run

- Version: `0.2.0-alpha.4`
- Build kind: local Test.app
- Environment: macOS arm64，test channel，SQLite schema 10
- Sample command: `npm run qa:synthetic-books`
- 隐私：测试 Key 只保存于 Test.app 独立 Keychain；以下记录不包含 Key、正文或本地绝对路径。

| ID | Result | Notes |
| --- | --- | --- |
| SYN-001 标准目录 96 页 | Pass | 13 个目录项；标题页/目录忽略，前置说明可读，8 个正文章，参考资料为附录 |
| SYN-002 异常前后置页 54 页 | Pass | 11 个目录项；空格标题、自序、参考书目、附录与出版历史用途识别符合预期 |
| SYN-003 无目录 72 页 | Pass | 通过版面标题识别出 6 个正文章 |
| SYN-004 大目录 220 页 | Pass | 21 个目录项；14 个正文章，参考资料与 2 个附录默认不读 |
| SMK-005 阅读器打开 | Pass | 标准样本滚动模式正常渲染；翻页模式完整显示并可前后翻页 |
| SMK-006 阅读进度 | Pass | 翻到本节第 2 页后立即显示并保存继续阅读位置 |
| AI 连接测试 | Pass | OpenAI-compatible / DeepSeek 连接成功，未记录凭据 |
| AI-003 完整导读 | Blocked | 本次模型响应超过 120 秒，未取得完整导读；不追加重试消耗 |
| AI-004 超时与取消 | Pass | 120 秒自动退出加载态并显示明确提示；正文仍可继续打开 |
| 旧计划小尾段迁移 | Pass | 《万历十五年》`45+3` 与 `45+1` 自动合并，计划恢复为连续 7 项 |

## 发布测试记录模板

每次发布可以在 issue、PR、release notes 草稿或 `docs/APP_EVOLUTION_LOG.md` 中记录：

```markdown
## QA Run

- Version:
- Build kind: dev / local / signed
- Date:
- Tester:
- Environment:
- Sample set:

| ID | Result | Notes |
| --- | --- | --- |
| SMK-001 | Pass |  |
| SMK-002 | Pass |  |
| SMK-003 | Pass |  |

## Blockers

- （无）

## Follow-ups

- （无）
```

## 后续 P6.10

- P6.10.2：建立可公开提交的最小 PDF/MOBI/备份 fixtures 或样本说明。已完成基础版。
- P6.10.3：建立升级样本：旧 schema 数据库、含书旧备份、含书新备份、损坏备份。
- P6.10.4：把部分 smoke test 自动化到 Playwright 或 Rust 测试。
